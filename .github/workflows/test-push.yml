name: Test Push

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Push translations
        uses: ./
        with:
          action: push
          api_key: ${{ secrets.TEAAAMS_API_KEY }}
